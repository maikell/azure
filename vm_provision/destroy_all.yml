---
 - hosts: localhost
   vars:
    az_resource_name: development
    az_resource_location: francecentral

   tasks:
     - name: Destroy resource group development
       azure_rm_resourcegroup:
        name: "{{ az_resource_name }}"
        location: "{{ az_resource_location }}"
        state: absent
        force_delete_nonempty: yes